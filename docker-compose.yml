version: "3.9"

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: dams-sqlserver
    restart: always
    environment:
      SA_PASSWORD: "SUA_SENHA_AQUI"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
